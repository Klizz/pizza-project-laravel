import React from "react";
import ReactDOM from "react-dom";

function Header() {
    return (
        <nav className="header">
            Pizza project
        </nav>
    );
}

export default Header;